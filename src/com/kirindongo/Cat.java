package com.kirindongo;

import java.time.LocalDate;

/**
 * Feeding schedule: daily
 */
public class Cat extends Animal{
		Cat(String name, Integer age, LocalDate birthday, LocalDate lastFed) {
				super(name, age, birthday, lastFed);
		}
}
